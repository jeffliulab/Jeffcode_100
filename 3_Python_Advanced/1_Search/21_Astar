启发式搜索算法
使用启发式函数（heuristic）指导搜索，常用于优化问题。

A 搜索*
描述：结合实际代价 
𝑔
(
𝑛
)
g(n) 和启发式估计代价 
ℎ
(
𝑛
)
h(n) 的总和 
𝑓
(
𝑛
)
=
𝑔
(
𝑛
)
+
ℎ
(
𝑛
)
f(n)=g(n)+h(n)，优先扩展 
𝑓
(
𝑛
)
f(n) 最小的节点。
时间复杂度：
𝑂
(
𝐸
)
O(E)（取决于启发式函数质量）
适用场景：路径规划、游戏 AI 等。